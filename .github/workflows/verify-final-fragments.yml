name: Inventory remaining explicit ignores

on:
  push:
    branches: [fix/final-fragment-contracts]

permissions:
  contents: read

jobs:
  inventory:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.x'
      - name: Print remaining ignores
        run: |
          cd checks
          python3 gen.py
          python3 - <<'PY'
          import json

          manifest = json.load(open('manifest.json'))
          rows = [
              info for info in manifest.values()
              if info.get('expect') == 'ignore'
          ]
          rows.sort(key=lambda info: (info['file'], info['line'], info['section']))

          print(f'REMAINING_IGNORES {len(rows)}')
          for info in rows:
              print(
                  f"{info['file']}\t{info['line']}\t{info['section']}\t{info.get('reason', '')}"
              )
          PY
