# Exact known failures of examples whose expectation is `compile`.
# Format: file :: section :: block N :: sorted(error-tokens) || reason
# Migration debt only; new signatures fail and stale entries must be removed.
